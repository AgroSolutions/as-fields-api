apiVersion: v1
kind: Service
metadata:
  name: as-fields-api-service
  labels:
    app: as-fields-api
spec:
  selector:
    app: as-fields-api
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  type: LoadBalancer
